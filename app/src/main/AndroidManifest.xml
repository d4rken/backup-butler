<?xml version="1.0" encoding="utf-8"?>
<manifest package="eu.darken.bb"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name="eu.darken.bb.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="eu.darken.bb.main.ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.LauncherToMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".onboarding.OnboardingActivity"/>
        <activity
            android:name=".task.ui.editor.TaskEditorActivity"
            android:theme="@style/BaseAppTheme"/>
        <activity
            android:name=".storage.ui.editor.StorageEditorActivity"
            android:theme="@style/BaseAppTheme"/>
        <activity
            android:name="eu.darken.bb.backup.ui.generator.editor.GeneratorEditorActivity"
            android:theme="@style/BaseAppTheme"/>
        <activity
            android:name=".settings.ui.SettingsActivity"
            android:theme="@style/BaseAppTheme"/>
        <activity
            android:name=".storage.ui.viewer.StorageViewerActivity"
            android:theme="@style/BaseAppTheme"/>

        <activity
            android:name=".debug.recording.ui.RecorderActivity"
            android:theme="@style/FloatingAppTHeme"/>

        <service
            android:name="eu.darken.bb.main.core.service.BackupService"/>
        <service android:name=".debug.recording.core.RecorderService"/>

        <provider
            android:name="androidx.work.impl.WorkManagerInitializer"
            android:authorities="${applicationId}.workmanager-init"
            android:exported="false"
            tools:node="remove"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths"/>
        </provider>
    </application>

</manifest>